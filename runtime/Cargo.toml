[package]
name = "runtime"
version = "0.1.0"
edition = "2021"

[dependencies]
bot_core = { path = "../bot_core" }
connectors = { path = "../connectors" }
tokio = { version = "1.0", features = ["full"] }
serde_json = "1.0"
